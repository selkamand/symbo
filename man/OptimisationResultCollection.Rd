% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{OptimisationResultCollection}
\alias{OptimisationResultCollection}
\title{Collection of optimisation results}
\usage{
OptimisationResultCollection(
  optimisations = list(),
  mol1_not_optimised,
  mol2_not_optimised
)
}
\arguments{
\item{optimisations}{A list of \code{\link{OptimisationResult}} objects,
each corresponding to the optimisation conducted for one shape class.
Defaults to an empty list.}

\item{mol1_not_optimised}{A \code{\link[structures:Molecule3D]{structures::Molecule3D}} object
representing the \emph{original}, unaligned version of molecule 1.
This is included in the collection so reports and summaries can
display both the starting geometry and the optimised structures.}

\item{mol2_not_optimised}{A \code{\link[structures:Molecule3D]{structures::Molecule3D}} object
representing the \emph{original}, unaligned version of molecule 2.}
}
\value{
A new \code{OptimisationResultCollection} S7 object containing the supplied
optimisation results.
}
\description{
\code{OptimisationResultCollection} is an S7 container object that stores one or
more \code{\link{OptimisationResult}} objects, typically corresponding to
different shape classes or binding modes evaluated in a single alignment /
docking run.

It provides:
\itemize{
\item a list-like slot \code{@optimisations} containing the individual
optimisation results; and
\item a read-only \code{@shapeclasses} property that extracts the
\code{shapeclass} field from each contained result.
}

A convenience \code{\link[base:as.data.frame]{base::as.data.frame()}} method is provided so that key
summary quantities (distance, angle, rotations, slides, axes) can be
inspected and compared in a tabular form.
}
\section{Fields}{


The class has the following properties:

\describe{

\item{optimisations}{A list of \code{\link{OptimisationResult}} objects.
The list may be empty. A validator enforces that every element in this
list is a valid \code{OptimisationResult}; otherwise an error is raised.}

\item{shapeclasses}{Character vector (read-only) giving the
\code{shapeclass} value of each element in \code{@optimisations}, in the
same order. This is computed on the fly via the getter and cannot be set
directly.}

}
}

\section{Typical usage}{


Instances of \code{OptimisationResultCollection} are usually constructed by
higher-level routines that evaluate multiple shape classes. Each shape
class produces an \code{\link{OptimisationResult}}, and these are collected
into a single object for printing, summarising, or coercion to a data frame.
}

\examples{
# Create a couple of dummy optimisation results
res1 <- OptimisationResult(shapeclass = "shapeA")
res2 <- OptimisationResult(shapeclass = "shapeB")

# Combine into a collection
coll <- OptimisationResultCollection(
  optimisations = list(res1, res2),
  mol1_not_optimised = structures::Molecule3D(),
  mol2_not_optimised = structures::Molecule3D()
)

# Print summary
coll

# Coerce to data.frame for further analysis
df <- as.data.frame(coll)
df$shapeclass

}
