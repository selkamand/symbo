% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maths.R
\name{rotate_vector_around_axis}
\alias{rotate_vector_around_axis}
\title{Rotate a vector around an axis by a given angle}
\usage{
rotate_vector_around_axis(v, rotation_axis, angle, tol = 1e-08)
}
\arguments{
\item{v}{Numeric length-3 vector to rotate.}

\item{rotation_axis}{Numeric length-3 vector; rotation axis (will be normalized).}

\item{angle}{Numeric scalar (radians); rotation angle.}

\item{tol}{Numeric; tolerance for early return check (\code{sum(v) < tol}).}
}
\value{
Rotated vector of same length as \code{v}.
}
\description{
Rotates a 3D vector \code{v} about \code{rotation_axis} by \code{angle} radians
using Rodrigues' rotation formula. Positive angles follow the right-hand rule.
}
\note{
\itemize{
\item Early return uses \code{sum(v) < tol}, which may skip rotation for some nonzero vectors.
\item Depends on user-defined \code{normalise()} and \code{pracma::cross()}.
\item No input validation; assumes 3-element numeric vectors and nonzero rotation axis.
}
}
\examples{
# Rotate (1,0,0) by 90° around z-axis -> (0,1,0)
v <- c(1,0,0); axis <- c(0,0,1)
rotate_vector_around_axis(v, axis, pi/2)

}
